# PMB Service Environment Configuration

# ===========================================
# LOCAL DEVELOPMENT (SQLite)
# ===========================================
# Run: bun run setup:local
# This will create local SQLite database automatically

PORT=3001
NODE_ENV=development
DATABASE_URL="file:./dev.db"

# ===========================================
# PRODUCTION (PostgreSQL) - Render.com
# ===========================================
# Set these in Render Dashboard Environment Variables:
#
# PORT=3001
# NODE_ENV=production
# DATABASE_URL=postgresql://user:password@host:5432/dbname
#
# Build Command: bun install && bun run db:generate && bun run db:migrate:deploy && bun run db:seed
# Start Command: bun run start
